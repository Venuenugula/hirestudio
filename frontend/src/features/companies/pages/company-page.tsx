import { PlaceholderPage } from "@/components/shared/placeholder-page"

export function CompanyPage() {
  return (
    <PlaceholderPage
      title="Company"
      description="Manage tenant branding, slug, and theme colors."
      emptyTitle="Company editor not ready"
      emptyDescription="Create and update company details will plug into the Company API next."
    />
  )
}
